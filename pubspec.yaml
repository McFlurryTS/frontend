name: McDonalds
description: "A new App for McDonald's"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  # Firebase
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.5
  flutter_local_notifications: ^19.1.0
  
  # Authentication
  flutter_login: ^5.0.0

  # Time
  timezone: ^0.10.0

  # Location & Maps
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  google_maps_flutter: ^2.5.3

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  curved_navigation_bar: ^1.0.6
  http: ^1.3.0
  provider: ^6.1.4
  flame: ^1.27.0
  skeletonizer: ^1.4.3
  google_fonts: ^6.2.1
  flutter_staggered_grid_view: ^0.7.0
  uuid: ^4.5.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  smooth_page_indicator: ^1.1.0
  flutter_cache_manager: ^3.3.1
  path_provider: ^2.1.2
  diacritic: ^0.1.5
  lottie: ^2.3.2
  cached_network_image: ^3.3.1
  carousel_slider: ^5.0.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.14.3
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.6

flutter_icons:
    android: true
    image_path: assets/icons/logo/72_logo.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/icon/
    - assets/icons/logo/
    - assets/images/trash_bins/
    - assets/images/
    - assets/happy/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # in this "flutter" section. Each entry in this list should have ae,
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. Forh a
  # example:ing the asset and other descriptors for the font. For
  # fonts:e:
  #   - family: Schyler
  #     fonts:: Schyler
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttff
  #         style: italicSchyler-Italic.ttf
  #   - family: Trajan Pro
  #     fonts:: Trajan Pro
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700s/TrajanPro_Bold.ttf
  #         weight: 700
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-packageendencies,
